/**
 * Marketplace content (saleable item, partner) involved with
 * Promoted delivery.
 */
 export type Content = {
  contentId?: string
  heroImageUrl?: string
  insertionId?: string
  name?: string
}
